# 프론트엔드 환경변수 예시
# 이 파일을 복사해서 .env.local 로 만든 다음 실제 값을 채워주세요
# cp .env.example .env.local

# ── 백엔드 API 주소 ───────────────────────────────────
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ── 카카오 지도 앱키 ──────────────────────────────────
# https://developers.kakao.com 에서 발급 (JavaScript 키 사용)
NEXT_PUBLIC_KAKAO_MAP_APP_KEY=f6d1343d63eb72fd28ac346f053ab931
